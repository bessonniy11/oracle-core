# Oracle Core: План реализации проекта

Правило: для каждого этапа тестирование должно быть успешно завершено перед переходом к следующему, кроме случаев, когда полноценно протестировать можно только после выполнения зависимых частей (указать в тест-плане этапа).

## Этап 1. Инициализация и архитектурная база
- [x] Настроить монорепозиторий и подмодули (`projects/backend`, `projects/frontend`).
- [x] Подготовить общие скрипты и линтеры (ESLint/Prettier для backend, формат/анализ для Flutter).
- [x] Оформить `.env.example` (API ключи, БД, CORS, лимиты).
- [x] Настроить CI черновой (lint + typecheck backend; статический анализ Flutter).
- [x] Документация: обновить README и ссылки на спецификации.

**Тестирование этапа 1 (обязательно):**
- [x] Прогон линтеров/форматирования (backend, frontend).
- [ ] Проверка сборки backend (tsc без ошибок) и генерации миграций в dry-run.
- [ ] Проверка сборки frontend (`flutter analyze`).
- [ ] Проверка, что подмодули и env-шаблоны доступны и корректно читаются.

## Этап 2. Бэкенд: данные и API каркас
- [ ] Настроить структуру слоёв (контроллеры/сервисы/репозитории).
  - [x] Настроить TypeORM, соединение с PostgreSQL, базовые миграции.
  - [x] Реализовать модели: персоны, правила, чаты, сообщения (ограничения из спецификации).
  - [x] CRUD персоны/правила (валидация, лимиты, is_default).
  - [ ] CRUD чатов и история (пагинация).
  - [x] Единый формат ошибок/логирование/correlation-id.
- [ ] Конфиг AI клиента и заглушка `POST /ai/completions` (пока без вызова).

**Тестирование этапа 2 (обязательно):**
- [ ] Unit/интеграционные тесты CRUD (успешные кейсы, валидация, лимиты, 404/400/429).
- [ ] Миграции: накатывание/откат на тестовой БД.
- [ ] Логи: проверка наличия correlation-id и формата ошибок.
- [ ] Нагрузочный sanity: пагинация (лимиты) на истории (smoke).

## Этап 3. Бэкенд: интеграция с Gemini
- [ ] Реализовать клиента Gemini (`gemini-2.5-flash`), конфиг из ENV.
- [ ] Обогащение контекста: системный промт персоны + правила + история чата.
- [ ] Обработка таймаутов/ошибок AI, маппинг в единый формат ошибки.
- [ ] Поддержка повторной отправки (`/chats/:id/messages/:msgId/resend`).
- [ ] Лимиты на длину входа/токенов, обрезка перед запросом.

**Тестирование этапа 3 (обязательно):**
- [ ] Контрактные тесты клиента Gemini (моки/фикстуры).
- [ ] Интеграционные тесты эндпоинта сообщений: успех, таймаут, ошибка AI, повтор.
- [ ] Проверка обрезки входов и корректных ответов об ошибках/лимитах.

## Этап 4. Бэкенд: голосовые сервисы (опционально)
- [ ] STT endpoint `POST /voice/stt` с лимитами размера/длины.
- [ ] TTS endpoint `POST /voice/tts` с обработкой ошибок и фолбеком на текст.
- [ ] Логи/метрики для STT/TTS.

**Тестирование этапа 4 (обязательно/по готовности):**
- [ ] Интеграционные тесты STT/TTS с моками внешних сервисов.
- [ ] Проверка лимитов аудио и сообщений об ошибке.
- [ ] Если внешние сервисы недоступны — отмечаем зависимость и переносим полное тестирование после их подключения.

## Этап 5. Фронтенд: каркас и базовые экраны
- [ ] Настроить базовый роутинг/навигацию (сплэш, чат, шторка, настройки, персоны).
- [ ] Настроить стейт-менеджмент (Riverpod) и DI.
- [ ] Имплементировать UI главного экрана: имя персоны, смайлик, история, ввод/отправка/отмена, статусы (думает/ошибка/оффлайн).
- [ ] Боковая шторка чатов: список, создание, удаление (подтверждение), пустое состояние.
- [ ] Настройки: список персон, пустое состояние, переход к созданию/редактированию.

**Тестирование этапа 5 (обязательно):**
- [ ] Widget-тесты главного экрана (пустое состояние, загрузка, ошибка, оффлайн).
- [ ] Widget/интеграционные тесты шторки чатов (создание, удаление, подтверждение).
- [ ] Widget-тесты настроек (список, пустое состояние).
- [ ] Сетевые моки: проверка обработки 400/401/429/5xx.

## Этап 6. Фронтенд: чаты, персоны и правила
- [ ] Экран создания/редактирования персоны: имя, промт, правила (CRUD), подтверждение удаления.
- [ ] Подгрузка/пагинация истории чата, кнопка “к последнему”.
- [ ] Обработка лимитов ввода (текст, аудио), сообщения пользователю.
- [ ] Голосовой ввод/вывод (кнопки, индикаторы) с фолбеком на текст.
- [ ] Голосовые команды (минимальный набор): новый чат, новое правило, смена персоны, стоп.

**Тестирование этапа 6 (обязательно):**
- [ ] Widget/интеграционные тесты форм персоны/правил (валидация, пустые состояния).
- [ ] Интеграционные тесты истории: пагинация, “к последнему”, большие истории (смоук).
- [ ] Тесты голосовых команд с моками STT/TTS; фолбеки при ошибках/оффлайн.
- [ ] Проверка ограничений ввода и пользовательских подсказок.

## Этап 7. E2E и стабилизация
- [ ] E2E сценарии: создать персону, создать чат, отправить текст, получить ответ, повторить запрос при ошибке.
- [ ] E2E сценарий с голосом (если включены STT/TTS).
- [ ] Проверка логов/метрик и алёртов на основных путях.
- [ ] Финальный проход по документации (README, спецификации, env).

**Тестирование этапа 7 (обязательно):**
- [ ] Прогон E2E тестов (web/mobile по доступности).
- [ ] Проверка алёртов/метрик/логов на ошибки.
- [ ] Ретест критических API и UI путей после фиксов (регресс).
